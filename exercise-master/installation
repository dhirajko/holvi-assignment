1 Install pycharm
2 Create VirtualEnv
    Pycharm -> Settngs -> Project Interpreter -> setting > ADD -> Virtual Env -> Select new env -> Okay
3 Close the terminal
4 Open terminal , and you should see the bracket in the left hand side of the terminal which is the python virtual env that you should be working on
5 Do pip install -r requirements.txt in the virtual environment


Create user
In the terminal type ipython
opens intereactive shell

from django.contrib.auth.models  import User
dhiraj = User(first_name='dhiraj', 'email'='sadfasf@adfas.com').save()

from fintech.models import Account, Transaction

dhiraj_savings_account = Account(name='savings', balance=34, user=dhiraj).save()
dhiraj_current_account = Account(name='savings', balance=34, user=dhiraj).save()


Create account
http://127.0.0.1:8000/accounts/create

POST
    {
	"name": "pension",
	"balance": "450",
	"user": 1
}


users
http://127.0.0.1:8000/accounts/users


accounts

http://127.0.0.1:8000/accounts